apiVersion: data.packaging.carvel.dev/v1alpha1
kind: Package
metadata:
  name: knative-serving.tkgdev.failk8s.com.develop
  namespace: kapp-controller-packaging-global
spec:
  refName: knative-serving.tkgdev.failk8s.com
  version: develop
  template:
    spec:
      fetch:
      - imgpkgBundle:
          image: quay.io/failk8s/knative-serving-package:develop
      template:
      - ytt:
          paths:
          - config/
      - kbld:
          paths:
          - '-'
          - .imgpkg/images.yml
      deploy:
      - kapp:
          rawOptions:
          - --wait-timeout=5m
